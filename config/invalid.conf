# ========================================
# INCORRECT CONFIGURATION - FOR TESTING ERRORS
# ========================================
# This configuration contains intentional errors
# to test the parser validations

server {
    listen 8080;
    iface 127.0.0.1;
    server_name test.local;
    
    root www;
    index index.html;
    
    # ERROR: Invalid boolean value (must be 'on' or 'off')
    auto_index maybe;
    
    # ERROR: Upload with invalid value
    upload_enable yes;
    
    # ERROR: Unknown directive
    unknown_directive some_value;
    
    # Mixed valid configuration
    allow_methods GET POST;
}
